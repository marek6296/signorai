# DEAKTIVOVANÝ — bot scheduling prebehol na Vercel Cron (vercel.json)
# Vercel cron spúšťa /api/admin/auto-pilot každú hodinu automaticky.
# Žiadny GitHub Actions nie je potrebný.

name: Full Automation Bot Cron (DISABLED)

on:
  workflow_dispatch: # Iba manuálne — žiaden automatický cron

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Boty bežia cez Vercel Cron (vercel.json). Tento workflow je deaktivovaný."
